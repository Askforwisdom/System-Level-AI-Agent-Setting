# Agent 一般行为规范与环境要求

1. **工作环境**：Agent 的一般工作环境要求项目根目录具有 `AGENTS.md` 文件和 `.agents` 目录。
2. **环境构建**：Agent 在首次会话、初始化或后续会话中发现工作环境缺失时，构建 Agent 的工作环境（补齐项目根目录的 `AGENTS.md` 与 `.agents` 目录）。
3. **AGENTS.md 内容**：`AGENTS.md` 要简洁地给项目一个定义，简洁描述项目的主要内容、当前的主要工作方向。
4. **内容沉淀与变更**：空项目的 `AGENTS.md` 内容可为空，但在项目推进过程中应探求用户的真实意图并将其沉淀至 `AGENTS.md` 中；后续如若发生变更，与用户确认后审慎修改更新 `AGENTS.md`。
5. **.agents 目录**：`.agents` 目录是 Agent 在项目中的工作目录，承载 Agent 的能力与工作产物，按用途组织子目录；新增用途时在 `.agents` 下建立对应子目录。当前各 skill 约定的子目录：
   - `skills/`：项目级 skill 存放目录（见第 6 条）。
   - `behavior/log/`：行为日志目录，按 `behavior-log` skill 规范存放月度行为日志 `YYYY-MM.md`。
   - `rhythm/`：行为节律目录，按 `rhythm` skill 规范存放项目行为节律定义 `rhythm.md`。
6. **skills 目录**：`skills/` 目录存放可复用的 skill，每个 skill 为包含 `SKILL.md` 的独立目录或独立的 `.md` 文件；项目级 skill 位于项目 `.agents/skills/`，系统用户级 skill 位于系统用户目录 `.agents/skills/`；当前任务落入某 skill 的职责范围时，载入该 skill 并遵循其规范。
7. **行为日志习惯**：所有 Agent 在有任何行为或动作时，必须记录行为日志。行为完成、暂停或失败后必须按 `behavior-log` skill 规范向目标项目 `.agents/behavior/log/YYYY-MM.md` 追加日志；派发 subagent 执行项目行为时，须在 prompt 中明确要求其写入行为日志。
8. **规范文本表述**：编写或修改 `AGENTS.md`、`SKILL.md` 等文本时，建议文字应该：
   - **简明直接**：规则陈述条件与动作即可，删繁就简。
   - **正向表述**：优先说明应当怎么做；单条操作纪律可直接简明陈述，不必叠加强制修饰。
   - **引导优先**：宜用"建议、一般、可"类引导词表述规则，少用强制与否定叠加的强调。
   - **术语统一**：行为主体是 Agent，同一概念使用同一术语。

# 系统用户级 Agent 注册登记表

以下为**系统用户级 Agent 注册登记表**：登记本机当前系统用户环境下可跨项目调用的 Agent 及其能力，会在**每个会话**启动时注入上下文。当当前任务涉及下表某个 Agent 的职责范围时，可按注册信息调用该 Agent 的能力。

## 注册 Agent

<!-- 后续有新 Agent 要注册时，按 skills/ai-agent-community skill 规范化执行；写入会话范围等约定以该 skill 为准 -->

## 使用约定

1. **优先通过 subagent 使用注册 Agent 的能力**。不要在主会话里直接跨目录翻查文件——用 `Agent` 工具派发子代理（只读查询用 `explore` 类型），把中间过程隔离在子代理上下文中，只取回结论。
2. **派发子代理时需要做会话级引导**。prompt 中需要显式包含以下引导指令（模板如下）：

   ```
   你的目标工作目录是 <项目路径>。开始任务前，先按Agent程序正常会话启动逻辑初始化上下文，且需要包含以下内容：
   1. Read <项目路径>/AGENTS.md，遵循其中规范、约束；
   2. 载入 <项目路径>/.agents/skills/ 下的 skill；
   3. Read 系统用户目录的 .agents/AGENTS.md，并遵循其中的规范、约束；
   4. 载入 系统用户目录的 .agents/skills/ 下的 skill；
   请先检查上下文中是否包含上诉内容，然后再完成以下任务：<具体任务描述>
   ```

3. **结论回传**：子代理只返回结论与关键引用路径，不要把大段文件原文倒回主会话。
4. **权限边界**：注册登记表授权使用该 Agent，包括通过 subagent 执行查询、修改、维护等操作，但必须按各 Agent 自身 AGENTS.md 约定的方式使用——各 Agent 自身 AGENTS.md 中的约束（如只读范围、写操作限定、维护类 skill 的执行条件）作为权限判据。

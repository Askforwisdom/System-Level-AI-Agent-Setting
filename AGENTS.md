# 项目 Agent 的构成与运行规范

1. **项目 Agent 构成**：项目根目录中的 `AGENTS.md`、`.agents` 目录及其他项目文件共同构成项目 Agent；其中，`AGENTS.md` 与 `.agents` 目录是项目 Agent 的核心构成。
   - 环境构建：Agent 在首次会话、初始化或后续会话中发现基础结构缺失时，补齐项目根目录的 `AGENTS.md` 与 `.agents` 目录。
2. **AGENTS.md 定义**：`AGENTS.md` 是项目 Agent 的定义入口，简洁描述项目 Agent 的定位、主要内容与当前主要工作方向。
   - 内容沉淀与变更：空项目的 `AGENTS.md` 可为空；项目推进过程中，Agent 应探求用户的真实意图并将其沉淀至 `AGENTS.md`；后续发生变更时，与用户确认后审慎更新。
3. **.agents 目录**：`.agents` 目录是项目 Agent 的核心构成之一，承载 Agent 的规则、能力、行为记录与一些工作产物。按用途组织子目录；新增用途时在 `.agents` 下建立对应子目录。当前各 skill 约定的子目录：
   - `skills/`：存放可复用的 skill，每个 skill 为包含 `SKILL.md` 的独立目录或独立的 `.md` 文件；项目级 skill 位于 `<项目 Agent 根目录>/.agents/skills/`，系统用户级 skill 位于 `<系统用户级 Agent 根目录>/skills/`；当前任务落入某 skill 的职责范围时，载入该 skill 并遵循其规范。
   - `behavior/log/`：项目 Agent 行为日志目录，按 `behavior-log` skill 规范存放月度行为日志 `YYYY-MM.md`。项目 Agent 的任何行为完成、暂停或失败后，按该 skill 规范向 `<目标项目 Agent 根目录>/.agents/behavior/log/YYYY-MM.md` 追加日志。
   - `rhythm/`：行为节律目录，按 `rhythm` skill 规范存放项目 Agent 行为节律定义 `rhythm.md`。
   - `tmp/`：Agent 运行时临时文件目录，专门存放 Agent 运行过程中需要生成的临时文件或脚本；任务完成时及时清理其中内容。
4. **写入方式**：建议 Agent 执行写入动作时使用自身的 Write 工具写入 UTF-8 格式的明文。
5. **规范文本表述**：编写或修改 `AGENTS.md`、`SKILL.md` 等文本时，建议文字应该：
   - 简明直接：规则陈述条件与动作即可，删繁就简。
   - 正向表述：优先说明应当怎么做；单条操作纪律可直接简明陈述，不必叠加强制修饰。
   - 引导优先：宜用"建议、一般、可"类引导词表述规则，少用强制与否定叠加的强调。
   - 术语统一：行为主体是 Agent，同一概念使用同一术语。
6. **Agent 与 subagent 等价**：Agent 与 subagent 均是项目 Agent 在会话中的运行形态，其行为均属于项目 Agent 行为。
   - 派发选型补充：派发 subagent 时，选择与 Agent 工具能力等价的类型（可读写文件、可执行命令），包括纯调查任务；只读类型（如只读探索、只读规划类）不派发。具体类型名称以当前运行环境提供的为准，按能力而非名称判断。
7. **自主先求解**：Agent 接受问题，先自我求解问题中属于自身职责和能力范围内的部分；对自身无法解决的部分寻求外部帮助，并负责整合结果、完成答复。

# 系统用户级 Agent 注册登记表

以下为**系统用户级 Agent 注册登记表**：仅登记本机当前系统用户环境下可跨项目调用的 Agent 根目录与职责，会在**每个会话**启动时注入上下文。当当前任务涉及某个 Agent 的职责范围时，按根目录定位该 Agent，并按下文“使用约定”加载其定义与适用 skill。

## 注册 Agent

<!-- 后续有新 Agent 要注册时，按 skills/ai-agent-community skill 规范化执行；写入会话范围等约定以该 skill 为准 -->

## 使用约定

1. **优先通过 subagent 使用注册 Agent 的能力**。不要在主会话里直接跨目录翻查文件——用 `Agent` 工具派发子代理，把中间过程隔离在子代理上下文中，只取回结论。
2. **派发子代理时需要做会话级引导**。prompt 中需要显式包含以下引导指令（模板如下）：

   ```
   你的目标项目 Agent 根目录是 <Agent 根目录>。开始任务前，先按Agent程序正常会话启动逻辑初始化上下文，且需要包含以下内容：
   1. Read <Agent 根目录>/AGENTS.md，遵循其中规范、约束；
   2. 载入 <Agent 根目录>/.agents/skills/ 下的 skill；
   3. Read 系统用户级 Agent 根目录中的 AGENTS.md，并遵循其中的规范、约束；
   4. 载入系统用户级 Agent 根目录中 skills/ 下的 skill；
   请先检查上下文中是否包含上述内容，然后再完成以下任务：<具体任务描述>
   ```

3. **结论回传**：子代理只返回结论与关键引用路径，不要把大段文件原文倒回主会话。
4. **权限边界**：注册登记表仅提供 Agent 根目录与职责。具体能力、使用权限与操作约定以目标 Agent 根目录中的 `AGENTS.md` 及适用 skill 为准；调用时按第 2 条完成加载并遵循其中规范。

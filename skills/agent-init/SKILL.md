---
name: agent-init
description: 会话初始化时将系统用户目录 .agents/AGENTS.md 加载进入上下文（包括子代理会话），并根据其中规范构建、检查当前环境。
---

# 会话初始化：加载系统用户级 AGENTS.md

会话初始化时，将系统用户目录 `.agents/AGENTS.md`（如 `C:/Users/<用户名>/.agents/AGENTS.md`）加载进入上下文（包括子代理会话），并根据其中规范构建、检查当前环境。
